# etl — EvoX Tensor Library

## Intent

A **minimal, explicit, compiler-neutral tensor graph runtime** (Python-first). etl provides an ergonomic tensor language for constructing, transforming, inspecting, caching, loading, and executing computational graphs. It deliberately does **not** own optimization, kernel scheduling, memory planning, or hardware code generation — those belong to external compilers (IREE, XLA via PJRT, TVM).

**Defining principle: Make computation explicit, require large tensors to be explicit inputs, keep `vmap` as transparent function-side sugar over `vectorize`, keep binding as transparent argument-passing sugar, keep communication and multi-device tensor preparation explicit, expose and persist the graph, and let compilers do the compilation.**

etl learns from Nx (explicit numerical language, `block`, `vectorize`, `runtime_call`, backend abstraction), early JAX (NumPy ergonomics, `vmap`/`grad`/`jvp`/`vjp`), PyTorch (interop), and tinygrad (small inspectable IR). The Python API stays semantically close to Elixir Nx so a future Elixir implementation can extend Nx rather than duplicate it.

## Design principles (binding for all submodules)

1. **The graph is the program.** `@etl.defn` marks a numerical graph definition — it is NOT JIT or eager execution. There is no implicit tracing and no eager/graph mode switching.
2. **Explicit staging.** Pipeline: `defn → trace → Graph → transform → Graph → lower → LoweredProgram → compile → CompiledArtifact → load → Executable → run → Tensor`. Every stage has an explicit public function. Convenience APIs (`etl.build`, `etl.evaluate`) are *documented shorthand* compositions only — no additional hidden semantics.
3. **Minimal magic.** No API silently traces, compiles, specializes, moves/reshards tensors, inserts collectives, executes Python callbacks, or switches semantics. Caching/binding are explicit operations. Sugar is allowed only as transparent composition of explicit primitives.
4. **Python value → Python semantics; SymbolicTensor → graph semantics.** `if etl.sum(x) > 0:` must fail clearly; runtime tensor control flow is `etl.cond` / `etl.while_loop` / `etl.scan`.
5. **Local tensors + explicit communication.** A tensor is always a local physical tensor. Collectives (`etl.dist.*`) appear explicitly in the program; the compiler may optimize but never invent communication. Multi-device helpers (`split_tensor`, `replicate_tensor`) are explicit data-preparation utilities, not sharding transformations.
6. **Large tensors must be explicit inputs.** Capturing a concrete `Tensor` from a closure into a graph is an error. `etl.constant(w)` is the only (explicit, warned) way to embed tensors. `etl.bind` is argument-passing sugar only.
7. **Frontend transformations are graph→graph.** `vectorize`/`vmap`/`grad`/`jvp`/`vjp` produce ordinary graphs of ordinary ops. Backends never need to understand `vmap` or Python containers.
8. **Compiler-neutral IR.** EvoXIR (region-based SSA) is the frontend IR. StableHLO is an important export target but not the definition of the IR. Backend limitations fail explicitly.
9. **No eager numerical implementation duplication.** Concrete creators (`etl.zeros`, `etl.tensor`, `etl.from_numpy`, …) and a reference CPU backend are the only concrete-computation paths; random generation, `linspace`, etc. go through compiled graphs.

## Value model (four distinct concepts)

| Concept | Meaning | Has storage? | Key capabilities |
|---|---|---|---|
| Python/static value | `None`, bool, int, float, complex, str, Enum, dtype, slice, config objects | n/a | Evaluated at trace time; specializes the graph. Changing it = new graph. No hidden guards/recompile. |
| `TensorSpec` | Describes a future runtime tensor | No | `shape` (symbolic dims allowed), `dtype`, optional `device`. Lifecycle: spec → trace → SymbolicTensor → run → Tensor. |
| `SymbolicTensor` | SSA value inside a graph | **No** | dtype, symbolic shape, source location, SSA identity. `x.numpy()`, `data_ptr()`, `__dlpack__` must NOT exist on it. |
| `Tensor` | Materialized runtime tensor | Yes | dtype, concrete shape, runtime/device, DLPack interop (`from_dlpack` / `__dlpack__`), `.numpy()`. Wraps numpy host memory (v1) or IREE/PJRT/TVM buffers (future). |

## Architecture overview

```
                         etl  (package)
        tensor language (ops, numpy namespace, trace, block)
        graph transforms (transforms: vectorize/vmap/grad/jvp/vjp)
        explicit collectives (dist)
                         │
                         ▼
                      EvoXIR (ir)
                         │
              ┌──────────┼───────────┐
              ▼          ▼           ▼
          StableHLO     native     (future: Relax/TVM, PJRT/XLA)
          (export)      numpy interpreter backend
────────────────── runtime boundary ──────────────────
                     Tensor  +  DLPack
```

**Pipeline objects (public types):** `Defn`, `Graph`, `LoweredProgram`, `CompiledArtifact`, `Executable`, `Tensor`. Each is a distinct public type; functions never silently consume an earlier-stage object and perform missing steps.

## Repository layout & routing table

| Path | Area |
|---|---|
| `./etl/` | The package. Public API contract lives in `./etl/CONTEXT.md` — read it before touching anything. |
| `./etl/sparse/` | Sparse tensor subsystem (value model, ops, transforms rules — see `./etl/sparse/CONTEXT.md`); numpy-backend-only in v1. |
| `./tests/` | pytest suite mirroring `./etl/` module structure + spec-compliance tests (design principles); sparse suite in `./tests/sparse/`. |
| `./pyproject.toml` | Packaging, deps (numpy only), pytest config. |

Cross-references: none yet (sibling dirs are read-only — escalate writes to parent).

## Cross-module contracts (binding)

**Import acyclicity (strict):** dependency layers, each importing only lower layers:
`core` ← `ir` ← `ops` ← {`trace`, `numpy`(enp)} ← {`block`, `transforms`, `backends`, `dist`, `sparse`, `pipeline`, `persist`}.
Details: `core` imports nothing from etl (numpy only). `ir` imports `core`. `ops` imports `core`, `ir`, `trace` (for the active-builder hook — `trace` must NOT import `ops`). `trace` imports `core`, `ir`. `block` imports `core`, `ir`, `ops`, `trace` (lazily for portable tracing). `transforms` imports `core`, `ir`, `ops`, `trace`. `backends` imports `core`, `ir` (plus lazy `ops`/`block` access for block-impl registration). `dist` imports `core`, `ir`, `trace`. `sparse` imports `core`, `ir`, `trace`, `ops`, `transforms` (rules register one-way into `transforms`' public registries; never imports `pipeline`/`backends` — `etl.evaluate` lazy-imports sparse inside the function). `pipeline` imports `trace`, `backends`, `core`. `persist` imports `core` only.

**Key shared contracts** (defined in `./etl/CONTEXT.md` and owned by the listed module):
- `core`: dtypes, `Dim`/`DimExpr` (symbolic shapes), `TensorSpec`, `Tensor`, `SymbolicTensor`, `Device`, TreeSpec + pytree utilities (`flatten`/`unflatten`, `tree_map`, `tree_leaves`, `tree_structure`, `tree_flatten`/`tree_unflatten` aliases, `register_pytree_node`), structure-mismatch contract (`first_mismatch_path`/`format_path`/`describe_node` — internal, used by trace/pipeline/transforms), device helpers (`devices`, `split_tensor`, `replicate_tensor`), plus a **registration hook** so `ops` can install `SymbolicTensor` operator handlers without import cycles.
- `ir`: `Value`, `Op`, `Block`, `Region`, `Function`, `Module`, `Builder`, `Location`, effect annotations, `verify()`, IR serialization.
- `ops`: frontend op functions (`add`, `dot`, `sum`, `reshape`, …) + `runtime_call`. Called with `SymbolicTensor` → builds IR into the active builder; called with concrete `Tensor` → `TraceError` (no eager mode, clear message suggesting `etl.constant` / explicit inputs / `etl.evaluate`); called outside a trace → `TraceError`.
- `trace`: `defn`, `trace`, `Graph`, `cond`, `while_loop`, `scan`, current-builder context, static-value snapshotting.
- `block`: custom ops — declaration, portable impl, per-backend impl registry, batching rules, derivative rules.
- `transforms`: `vectorize`, `vmap`, `grad`, `jvp`, `vjp` — graph→graph.
- `backends`: `Backend` interface (`lower`/`compile`/`load`/capabilities), registry (lazy activation of optional adapters), numpy interpreter backend (default, CPU), stablehlo exporter (MLIR text — also the shared lowering input for compiler backends), and pluggable compiler adapters `iree` / `xla` (PJRT C API via ctypes — user-provided plugin `.so`, no jax/jaxlib) / `tvm` (jaxlib only for its bundled MLIR bindings — the `jax` package is never imported) in `etl.backends.adapters` — optional, activated by `lower(..., backend="iree"|"xla"|"tvm")`, explicit errors when absent (pip hints for iree/tvm; `ETL_PJRT_PLUGIN`/`plugin_path` guidance for xla).
- `dist`: `group`, collectives (`all_reduce`, `all_gather`, `reduce_scatter`, `all_to_all`, `broadcast`, `collective_permute`), `rank`/`world_size` graph scalars.
- `sparse`: sparse tensor value model + 16 graph ops + per-op batching/vjp/jvp rules — full surface in `./etl/CONTEXT.md`; numpy-backend-only in v1 (compiler backends defer explicitly).
- `pipeline`: `LoweredProgram`, `CompiledArtifact`, `lower`, `compile`, `load`, `run`, `bind`, `build`, `evaluate`.
- `persist`: versioned/self-describing/integrity-checked save-load container, explicit `Cache`.

## Error strategy (binding)

All public errors derive from `ETLError` (in `core`): `TraceError`, `ShapeError`, `TransformError` (axis mismatch, unsupported batching/diff — never silent fallback), `BackendError`, `PersistenceError`, `DeviceError`, `DTypeError`, `VerificationError`. No API silently swallows or works around errors. Error messages include source location (e.g. `model.py:83`) whenever a graph location exists.

## Dependencies, testing, and hardware policy

- **Deps:** `numpy` is the only hard runtime dependency. Pure Python — no native C/Rust code in the library. Optional extras: torch (DLPack interop tests; `bench` — required only when running `etl.bench` comparisons against PyTorch), IREE/TVM (future adapters).
- **Testing:** pytest, CPU only. Tests mirror package structure under `./tests/`. torch interop tests use `pytest.importorskip("torch")`. Spec-compliance tests live in `./tests/test_spec_compliance.py` (closure capture errors, SymbolicTensor has no `.numpy()`, staging explicitness, bind is sugar, vmap=vectorize sugar, etc.).
- **GPU policy:** v1 never requires GPU — the reference backend is a CPU numpy interpreter. GPU is only considered later for optional adapters; if ever used, scan for an empty GPU first and do not occupy it for long.

## Non-goals (etl core must NOT contain)

NN modules, layers, optimizers, datasets, loaders, training loops, model zoo; automatic global sharding/resharding/implicit collectives; CUDA allocator / GPU runtime / kernel scheduler; compiler optimization passes; Triton compiler; distributed job scheduler, checkpoint manager, model serving. Separate libraries build those on top.

## Status

Implemented and tested. The full explicit pipeline (`trace → lower → compile → load → run` plus `bind`/`build`/`evaluate` sugar), graph transforms (`vectorize`/`vmap`/`grad`/`jvp`/`vjp`), explicit collectives (`dist`), custom blocks (`block`), the `etl.numpy` graph namespace, the numpy interpreter backend (default, CPU), the StableHLO exporter, the pluggable compiler adapters (IREE — CPU and CUDA validated end-to-end: `target_backends=["cuda"]` compile option + `Device("cuda", N)` load; **device-resident execution** — adapter outputs are device `Tensor`s wrapping the iree buffers with lazy `.numpy()`, same-device input `Tensor`s pass through with zero host round-trips, host inputs use a cached staging fast path; per-call pass-through ~0.13 ms on cuda (see `./etl/backends/adapters/CONTEXT.md`); XLA via the PJRT C API — a ctypes driver over a user-provided plugin `.so`, no jax/jaxlib, plumbing validated via a compiled test-plugin, real-XLA parity pending a real plugin; TVM with the `jax` package removed — jaxlib only for its bundled MLIR bindings — `etl.backends.adapters`, lazy optional extras, CPU-validated end-to-end with numpy parity where the compiler is present), and persistence/caching (`persist`, Graph save/load) are in place. A standalone conformance & benchmark harness (`etl.bench`) ships in the package: **97 curated example programs in three groups** — `op` (73 operation-level examples, one or two ops/transforms each: control flow `cond`/`while_loop`/`scan`, matmul variants, elementwise/reduce/structural ops, grad-rule coverage incl. erf/gelu/cumsum/tril/triu and the zero-vjp class, vmap variants incl. nested vmap, custom blocks via `etl.block` — the native user-defined-kernel mechanism, with per-backend impls + batching and jvp/vjp rules — and an xla-targeted set), `block` (17 small-block examples: transformer/attention/FFN/MLP/resnet-conv blocks, LSTM/GRU cells, one n-body step, one PSO/k-means/ALS/CG/power-iteration step), `e2e` (7 end-to-end programs for performance benchmarking: training loops — two transformer blocks trained on dummy data for 10 SGD iterations, MLP training, convnet training (convs frozen — conv VJP is a v1 deferral) — plus in-graph `while_loop` inference/PSO/k-means/power-iteration) — all staged through the explicit pipeline and compared against pure-numpy references (and PyTorch references when torch is available), reporting precision (conformance: max-abs/max-rel vs per-example or global tolerance) and speed (benchmark: best-of-N ms + speedup ratios); CLI via `python -m etl.bench`. Examples carry sub-group **tags** (16: micro, grad, vectorize, large, basic, control-flow, vmap, custom, xla, transformer_block, rnn, conv, mlp_block, optimization, train, infer); the `--examples` filter accepts names, categories, AND tags (precedence: category → exact name → tag, so exact example names always win). `Example` gained `tags` and `runner` fields — the runner is a Python-level multi-run procedure (build once, run N times) used by the e2e training examples, since grad-through-control-flow is a v1 deferral and training loops live outside the graph. Validated: numpy/cpu conformance **97/97 PASS in ~12 s** (default full run); iree spot-validation: `cond`/`while_loop` graphs compile and run on iree (incl. an in-graph while e2e, max_abs ~1e-7); `scan` desugars at trace time into while+gather+scatter (all v1), so scan graphs also compile and run on iree-llvm-cpu; custom-block (`block_call`) examples still defer with explicit `BackendError` (never silent fallback), and the vmap softmax/layernorm/attention keepdims-reshape deferral is unchanged. Per-example tolerances relax only where measured (grad-family rtol=atol=1e-3, e2e training 1e-3, mlp 1e-4, transformer 1e-3, conv2d_large 2e-4, nbody 2e-5, matmul_1024 5e-4 — fp32 accumulation-order noise; IREE is often closer to fp64 ground truth than the numpy fp32 reference). Torch is strictly optional — imported lazily only inside torch-comparison paths, never at module scope. **Sparse tensor support** (`etl.sparse`, numpy-backend-complete): three-phase value model — `SparseTensor`/`CSRTensor`/`CSCTensor` (concrete), `SparseTensorSpec` (trace inputs), symbolic instances inside graphs — all ONE registered pytree node (tensor leaves + `dense_shape`/dtype/format static leaves, polymorphic unflatten, canonical-form validation in constructors and kernels); polymorphic creators/converters (`coo`/`csr`/`csc`/`from_dense`, `to_dense`/`to_csr`/`to_csc`/`to_coo`); 16 graph ops (`sparse_from_dense/to_dense`, COO↔CSR/CSC conversions, `negate`, union `add`, intersection `multiply`, `multiply_dense`, `reduce_sum` → dense, `transpose`, static `reshape`, `concatenate` axes 0/1, `matmul` sparse@dense + dense@sparse) with native batch-aware numpy kernels (runtime structure validation, explicit `ShapeError`/`VerificationError`); per-op batching rules + vjp rules (13 ops; 3 documented `TransformError` deferrals) + explicit jvp rules for the bilinear ops — so `vmap`/`vectorize`/`grad`/`jvp`/`vjp` all work on sparse values (batched `dense_shape` gains the level-distinct batch `Dim` via `transforms.batching.register_batched_aux_remap`; the callable vmap path supports registered-node args); sparse values flow through `cond`/`while_loop`/`scan`; `etl.evaluate` derives `SparseTensorSpec` from concrete sparse args, leaf-level `bind` and Graph save/load work. Compiler backends (stablehlo/iree/tvm/xla) defer with explicit `BackendError` — see v1 deferrals. **Key-based functional RNG** (`etl.random`, numpy-backend-complete): rank-0 int64 key tensors (64-bit state, `key(seed)` masks to 64 bits) + pure graph ops `key`/`split`/`split_n`/`uniform`/`normal`/`randint`/`permutation`/`multinomial` — deterministic SplitMix64 streams with fixed per-op salts (same key + operands ⇒ BIT-IDENTICAL values across runs; ops never consume/mutate a key; `split`/`split_n` are sugar over the `random_key_mix` op); 6 OpDefs (category "random", effect "pure", 118 total) with numpy kernels in `etl/backends/numpy/kernels/random.py`; symbolic operands broadcast (symbolic `high`/`low`/`mean`/`std`, symbolic `n` for permutation); dtype rules (uniform/normal float, randint/permutation int, defaults float32/int32); `key()` polymorphic (concrete Tensor outside a trace, Constant inside), all other functions graph ops (`TraceError` outside a trace, concrete Tensor operands raise); works inside `cond`/`while_loop`/`scan` regions (key threaded via split). Creation ops default to `etl.float32` (consistent with `TensorSpec`). Compiler backends export 5 of the 6 random ops (`random_key_mix`/`random_uniform`/`random_normal`/`random_randint`/`random_permutation`) as v1 StableHLO — inline SplitMix64 i64 expansion in `etl/backends/stablehlo/random_export.py` (never `stablehlo.rng`; same key ⇒ bit-identical draws: uniform/randint/permutation EXACT vs numpy; random_normal with f32 output uses a documented f32 Box–Muller fast path on compiler backends (maxdiff ~3.6e-6 vs the f64 numpy kernel at 4096×50, same-key bit-identical across runs), f64/f16 output keeps the exact f64 math); only `random_multinomial` defers with explicit `BackendError`. Transforms have no rules for random ops → `TransformError`. **15-op batch** (`sort`/`argsort`/`topk`/`tile`/`stack`/`flip`/`roll`/`clamp`/`eye`/`matmul`/`cumprod`/`diag`/`isnan`/`nan_to_num`/`linspace`, numpy-reference end-to-end): 8 new IR ops (`sort`, `argsort`, `tile`, `flip`, `roll`, `diag`, `cumprod`, `nan_to_num` — the registry holds 118 op defs incl. `return`); the other 7 are frontend compositions (`topk` = sort/argsort + gather, `stack` = reshape + concatenate, `clamp` = maximum/minimum, `eye`/`linspace` = Constant compositions, `matmul` = dot sugar, `isnan` = comparison composition). Compiler backends export `sort`/`argsort`/`tile` as v1 StableHLO and defer `flip`/`roll`/`cumprod`/`nan_to_num` with explicit `BackendError` (compositions run via their components); `diag` is v1 via a flatten+gather (rank-2) / iota+select (rank-1) composition; no vjp/batching rules for the 8 new IR ops + `eye`/`linspace` (`TransformError`, the random-op pattern). Design notes: `./etl/CONTEXT.md`, `./etl/ops/CONTEXT.md`, `./etl/ir/CONTEXT.md`. Test suite (`./tests/`): **5512 passed, 18 skipped, 0 failed (torch/compiler-gated)**; sparse suite in `./tests/sparse/` (358 tests) + `TestSparseExplicitness` (21) in `test_spec_compliance.py`; `etl.random` suite in `tests/ops/test_random.py` (64 tests); 15-op regression suites in `tests/ops/test_sorting.py` (61), `test_structural.py` (78), `test_numeric.py` (71), `test_matmul.py` (41), the 101-op error table in `test_errors.py` (240), and the new iree emitters parity suite `tests/backends/test_iree_emitters_parity.py` (41: gather/sort/argsort/argmin/argmax/tile/scatter + bit-exact random + scan-through-while + shift parity on iree-llvm-cpu, incl. 5 iree-cuda smoke tests on `Device("cuda", 5)`, GPU-guarded) plus a 15-test shift suite in `tests/ops/test_elementwise.py`, and the device-resident semantics suite `tests/backends/test_iree_device_resident.py` (8: payload-backed run-outs with lazy `.numpy()`, same-device pass-through with ZERO `asdevicearray` calls, host numpy inputs, bit-exactness vs numpy, on llvm-cpu + iree-cuda, GPU-guarded with an nvidia-smi free-device scan) + 12 core payload-protocol tests in `tests/core/test_tensor.py` (dummy-payload: fresh-copy `.numpy()`, metadata-only `__eq__`, `DeviceError` dlpack, device derivation). skips depend on torch presence — 12 without torch in this env (7 torch-interop + 4 `etl.bench` torch-present tests; an xla-contract module may additionally skip when no PJRT plugin/gcc toolchain is available), 3 with torch installed (2 `etl.bench` torch-absent inverse tests + 1 xla-contract module). **Torch CPU/GPU validation** (torch 2.5.1+cu121, 8× RTX A6000, seed 0): CPU run (numpy backend, `--torch`, conformance+benchmark) EXIT 0 — 97/97 PASS, 61/61 torch refs PASS, 36 no-ref examples skipped (never errors); GPU run (iree/cuda, `target_backends=["cuda"]`) — 64/64 checked PASS incl. 44/44 torch-cuda comparisons, the remaining documented StableHLO v1 export deferrals, 2 upstream iree cuda HAL crashes on SPECIFIC while-loop shapes (`while_fib` segfaults, `while_cond_combo` dealloca ABORT — both pass on llvm-cpu; exclude `while_fib` from iree/cuda runs) — the fragility is shape-specific: an NSGA2-shaped while_loop (3 carries incl. an input-derived vector carry, `etl.select` on carried vectors) compiles and runs bit-exact on both llvm-cpu and iree-cuda — but that is a hand-shaped PROXY: the REAL evox non-domination-ranking while_loop and the full NSGA2 tell graphs segfault iree-compile intermittently (~9/10 llvm-cpu, ~3/5 cuda) and are BLOCKED on both targets, along with a deterministic cuda argsort (2-op sort, sorted axis ≥ 32) bufferization failure — see `./etl/backends/stablehlo/CONTEXT.md` and `./etl/backends/adapters/CONTEXT.md` Known Issues. Notable speedups: etl-numpy beats torch-cpu on small/grad graphs (`conv2d` 15×, `vmap_grad_mlp` 1.8×), torch wins on heavy compute (`matmul_1024` 0.47×, `transformer` 0.27×); etl-iree-cuda beats torch-cuda on `grad_mix` 11× but trails on matmul/transformer (~0.1×). The StableHLO exporter now equalizes mixed-dtype operands for elementwise/compare/select (commit 6aec043), so the int-scalar-promotion family (e.g. `grad_bitwise_zero`) compiles and runs on compiler backends with numpy parity — regression-pinned in `./tests/backends/`: `test_mixed_dtype.py` runs the six mixed-dtype blast-radius graphs (add/equal/select/bitwise_and/add/add over i32/i64/bool/i8/i16/f32/f64) on ALL THREE compiler adapters — iree + tvm validated in-env (12 tests), xla gate-skipped (6 tests) until a user-provided PJRT plugin is configured (`ETL_PJRT_PLUGIN`/`plugin_path`); plus 7 StableHLO export goldens in `test_stablehlo.py`. The shift primitives `etl.bitwise_left_shift`/`etl.bitwise_right_shift` (numpy dtype-natural: arithmetic on signed, logical on unsigned, bool → `DTypeError`) export as `stablehlo.shift_left` / `shift_right_arithmetic` (logical for unsigned results) with iree-llvm-cpu parity. **eigh/diag StableHLO v1 export** (commit 25e395a + the finishing test package): `eigh` composes as an unrolled cyclic-Jacobi eigensolver (8 sweeps of the (p, q) rotation pairs built from slice/iota/compare/select/elementwise — no while-loops) + a stable pair-sort for the ascending eigenvalue order + a gather column reorder of V, since StableHLO 1.0 removed `stablehlo.eigh`/`qr`/`svd` (iree 3.11 ships only `cholesky`); dtype rule mirrors the numpy kernel (int/bool → f64; f32/f64 pass; complex/f16 → explicit `BackendError`; dynamic dims → explicit `BackendError`); parity vs LAPACK numpy is fp32-tolerance, NOT bit-exact (10x10 f32 measured w 1.5e-5 / reconstruction 1.7e-5 / orthogonality 6e-7; f64 ~2.7e-15; int32 exact at the f64 floor) — tests assert ascending w, `A ≈ V diag(w) Vᵀ` and `VᵀV ≈ I`, never elementwise v (degenerate-eigenspace eigenvectors are basis-dependent); the 10x10 (CMAES-size) build+run takes ~17 s (5 f32 sweeps). `diag` exports as flatten + constant-index gather (rank-2 → main diagonal) or iota-EQ mask + select (rank-1 → diagonal matrix), dtype preserved incl. complex, EXACT vs numpy. Pinned by the export goldens in `test_stablehlo.py` (composition markers, dtype rules, dynamic-dims rejection) and the 8-test iree-llvm-cpu round-trip suite `tests/backends/test_iree_eigh_diag_parity.py` (3x3 / 10x10 / batched (2,3,3) f32, f64 3x3, int32→f64 upcast; complex diag is export-only — the iree adapter's dtype gate excludes complex64). See `./etl/CONTEXT.md` for the package-level public API contract and `./tests/CONTEXT.md` for the test strategy.

**v1 deferrals (intentional, explicit errors — never silent fallback):** control-flow vectorization; `conv` VJP (no transposed-conv op); symbolic-length `scan`; `linalg.inv` / `norm` / `det`; non-constant pad modes; symbolic-bound `arange` / `linspace`; a few StableHLO op exports (`cumsum`, `diagonal`, `erf`/`gelu`, the linalg factorizations `cholesky`/`qr`/`matrix_rank`/`svd`/`matrix_exp`, `random_multinomial`, … — `eigh` is v1 via an unrolled cyclic-Jacobi composition, see the Status section). Sparse-specific v1 deferrals (explicit errors, never silent): **sparse ops are numpy-backend-only** — all 16 `sparse_*` ops defer on compiler backends (stablehlo `export` and adapter `lower()`) with an explicit `BackendError` naming the op and suggesting densify via `etl.sparse.to_dense`; `sparse@sparse` matmul, sparse constants, whole-sparse `bind`, and `sparse_concatenate`/`sparse_coo_to_csc`/`sparse_csc_to_coo` vjps are also v1 deferrals. Random-specific v1 deferrals (explicit errors, never silent): only `random_multinomial` — of the 6 `random_*` ops (etl.random family) — defers on compiler backends (stablehlo `export` and adapter `lower()`) with an explicit `BackendError` naming the op; the other 5 random ops export as v1 StableHLO (inline SplitMix64 expansion in `etl/backends/stablehlo/random_export.py`, bit-exact vs the numpy kernels — see Status). `vmap`/`grad`/`jvp`/`vjp` have no rules for random ops → `TransformError` (never a silent Python-loop fallback). Compiler-adapter limitations (all explicit `BackendError`, never silent fallback — see `etl/backends/CONTEXT.md` Known Issues): collectives and `runtime_call` are not compiled by any adapter in v1 (multi-rank runtime channels not yet wired); symbolic/dynamic dims are supported PER-OP by the StableHLO exporter — elementwise/cast/select/reductions-over-static-dims/transpose/concat/dot (incl. dynamic batch broadcast and shared-weight vmap dots) work on symbolic shapes, while reshape/conv/slice/pad/reduce_mean-over-a-dynamic-reduced-dim with dynamic dims are rejected with a clear `BackendError` at `lower()` time (this is why vmap of softmax/layernorm/attention-style per-sample functions defers on compiler backends; the numpy backend handles all symbolic shapes). Vectorize/vmap batch dims are level-distinct (`batch`, `batch_1`, … — nested vmap with unequal extents works); mapped inputs of one pass share one `Dim`. Adapters are validated on CPU (IREE additionally on CUDA).

**Tree/pytree UX (high-level user experience):** `core` ships a complete tree utility API — `tree_map` (single- and multi-tree, strict structure validation with first-mismatch-path errors), `tree_leaves`, `tree_structure`, and `tree_flatten`/`tree_unflatten` (literal aliases of `flatten`/`unflatten`) — exported at top level (`etl.tree_map`, …). Structured data support: list/tuple/dict/namedtuple/dataclass (+ `defaultdict`/`Counter` roundtrips preserving `default_factory`; custom containers via `register_pytree_node`, which rejects `object`). Checks & feedback: structure-mismatch errors from `etl.run`/`etl.bind`/graph input validation report the first diverging pytree path with node descriptions (`expected dict with keys ['b', 'w'], got dict with keys ['b', 'w2']`); unorderable dict keys, dataclass `InitVar`/`init=False` rebuild failures, and unpreservable `default_factory` raise explicit actionable `TypeError`s; `Dim`/`DimExpr`/`Device` are single static leaves (trace inputs snapshot them as one static value, consistent with `core.flatten`); block portable impls may return namedtuple/dataclass pytrees. The duplicated structure-walkers in graph/pipeline/vectorize/grad were consolidated onto `core.first_mismatch_path` + `core.format_path` (+ `core.describe_node` for message rendering).
