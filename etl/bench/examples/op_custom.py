"""Custom-block op conformance examples (category "op", tag "custom") —
placeholder.

Filled by a later phase (``etl.block`` custom ops with impls/batching and
derivative rules); see :mod:`etl.bench.examples.base` for the
:class:`Example` contract and self-registration pattern.
"""
