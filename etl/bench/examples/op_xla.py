"""XLA-targeted op conformance examples (category "op", tag "xla") —
placeholder.

Filled by a later phase (examples staged on the pluggable XLA/PJRT adapter);
see :mod:`etl.bench.examples.base` for the :class:`Example` contract and
self-registration pattern.
"""
