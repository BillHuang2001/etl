"""End-to-end training examples (category "e2e", tag "train") — placeholder.

Filled by a later phase with multi-run procedures driven through the
optional ``Example.runner`` factory (see the runner contract in
``etl.bench._util``'s docstring); see :mod:`etl.bench.examples.base` for the
:class:`Example` contract and self-registration pattern.
"""
