# tests/sparse — etl.sparse test suite

## Intent

pytest suite validating the `etl.sparse` contract (`../../etl/sparse/CONTEXT.md` — sibling, read-only): the three-phase sparse value model (`SparseTensor`/`CSRTensor`/`CSCTensor`/`SparseTensorSpec`, one registered pytree node), polymorphic creators/converters, the 16 graph-time computation ops, canonical-form enforcement (concrete constructors AND runtime kernels), the batching/VJP/JVP rules (incl. the batched-output aux remap and the cotangent-leaf contract), sparse loop carries through cond/while_loop/scan, the explicit pipeline (evaluate spec derivation, leaf-level bind, static-leaf run validation, save/load), and the v1 deferral surface (explicit errors, never silent). Tests are the executable spec of the CONTEXT.md contract; concrete creators + the numpy backend are the reference for correctness.

## Structure

| File | Covers |
|---|---|
| `conftest.py` | Shared helpers: canonical 3×4 COO/CSR/CSC examples + dense reference (`dense_example`/`coo_example`/`csr_example`/`csc_example`), matching unbatched specs (`coo_spec`/`csr_spec`/`csc_spec`), `run_graph` (explicit lower→compile→load→run), `materialize`/`eval_dense` (sparse results → dense ndarrays), `batched_coo_example` (input-side batched leaves via `from_parts`) |
| `test_value.py` | Constructors (coo/csr/csc/from_dense concrete, int64 index normalization, core.Tensor leaves), canonical validation error matrix (every ShapeError/DTypeError/ValueError from value.py), `SparseTensorSpec` constructor + `from_concrete`, the one-pytree children layout (`[tensor leaves…, *dense_shape, dtype, format]`, TreeSpec.type == SparseTensor for every variant, polymorphic unflatten, dtype-leaf DTypeError), `is_sparse` across phases, concrete layout helpers (to_dense/to_coo/to_csr/to_csc incl. Dim-at-0 batched to_dense) |
| `test_errors.py` | Three-option TraceError on concrete sparse/dense operands inside a trace (all computation ops), spec-phase TypeErrors (TensorSpec creator components → "use a SparseTensorSpec", mixed kinds, converters on specs), run-time kernel canonical validation via the symbolic path (unsorted/duplicate/out-of-range COO, indptr violations, values-aware stored-zero tolerance PASSES), rank-2-only CSR/CSC→COO trace-time ShapeError |
| `test_ops.py` | Every computation op vs pure-numpy dense references (add/subtract union, multiply intersection incl. disjoint supports, multiply_dense, negate, sum/reduce_sum → dense, transpose, reshape, concatenate axes 0/1, matmul both directions + dense@dense→`dot`), mixed-format auto-conversion (exact emitted op lists for csr/csc→coo, coo→csr/csc, identity cases), from_dense/to_dense roundtrips, empty-nnz edge cases, trace-time static error matrix, IR op-name spot checks (all 16 names) |
| `test_backends.py` | numpy backend is the only `sparse_ops=True` backend; explicit-pipeline parity; capability-drift `BackendError` at `lower()` time (no compiler installed) for all 16 sparse ops on `backend="iree"` (+ 3 on `"tvm"`), StableHLO export naming errors, dense-only positive control on `"iree"` |
| `test_transforms.py` | Graph-level vmap/vectorize numerics (bare int axes + pytree `(0, 0)`; batch-Dim static-leaf checks: output dense_shape gains `Dim('batch')` at position 0, input statics unchanged; run results to_dense → (B, 3, 4)); container-axes and mixed-(0, None) pair-lead TransformErrors; out_axes; grad (`argnums=(1,)`, values gradient vs dense weights); vjp weighted cotangents + dot one-hot ACCUMULATION; the 3 vjp TransformError deferrals; jvp bilinear product rules (gather-based multiply) with zero-tangent short-circuits; the pre-existing vmap∘grad "cannot batch op 'gather' with dynamic index dims" gap pinned as a documented known limitation (no BUG marker) |
| `test_control_flow.py` | Sparse through cond (sparse- and dense-returning branches), while_loop (sparse carry + (sparse, dense) tuple carry, static-leaf snapshotting), scan (sparse carry + dense xs numerics), scan-over-sparse-xs deferral + sparse-y-stacking deferral (exact TraceError messages) |
| `test_pipeline.py` | Explicit pipeline + build/evaluate sugar (concrete sparse args derive SparseTensorSpec), from_concrete leaf alignment, leaf-level bind (indices/indptr leaves; values-leaf guard pinned), whole-sparse bind failure, Graph.save/load roundtrip (tmp_path), run-time static-leaf validation (wrong dense_shape/dtype/format fail loudly), stage-type discipline |
| `test_deferrals.py` | Cross-cutting explicit-error pins: no sparse constant, sparse@sparse matmul, whole-sparse bind, one representative capability-drift + one stablehlo naming error (mechanism pins — full 16-op matrix lives in test_backends.py), the 3 vjp deferrals, scan-over-sparse-xs |

## Constraints

- Siblings (`../../etl/`, other test dirs) are READ-ONLY — never modify etl to make a test pass. A real contract violation stays failing with a `# BUG(etl): <description>` marker + minimal repro and gets reported to the parent.
- CPU only, small shapes (canonical 3×4, a few ≤ (8, 16) cases). Each file < ~2s (test_backends.py is the slowest, ~1.2s in this env). No network, no GPU.
- All error tests use tight `pytest.raises(..., match=...)` regexes pinned to the exact wording in `etl/sparse/value.py` / `ops.py` / `rules.py` and the numpy kernel messages (`"kernel for op 'sparse_X': …"`).
- `python3 -m pytest tests/sparse` is green except the deliberately-kept failing BUG(etl) test (below).

## Known issues

- **BUG(etl) — concrete COO constructor is NOT values-aware for stored-zero duplicates** (`test_value.py::test_stored_zero_duplicate_row_constructs`, kept failing with a `# BUG(etl):` marker): the contract (`etl/sparse/CONTEXT.md`: "duplicate NONZERO rows error; duplicate rows with one stored zero are legal") promises the concrete constructor accepts a duplicate row whose pair includes a stored zero; `etl/sparse/value.py:254` rejects ALL duplicate rows (`"COO indices must be unique (no duplicate rows)"`). The runtime kernels ARE values-aware (pinned passing in `test_errors.py::test_runtime_stored_zero_duplicate_passes`). After an etl fix, delete the marker comment and this entry.

## Notes for agents

- **Pytree layout gotcha:** flatten children are `[tensor leaves…, *dense_shape, dtype, format]` — static leaves are `[*dense_shape, dtype, format]` (one leaf per dense dim). The FORMAT leaf (last) and dtype leaf (second-to-last) are the ones the pipeline snapshots; coo-vs-csr run mismatches fail at `Graph.flatten_inputs` with a path like `[0]`.
- **Graph-level vmap is the supported sparse path:** the callable path `etl.vmap(f)(sparse_spec)` fails in v1 (spec stripping produces invalid leaf shapes) — do NOT test it. Trace with an unbatched spec, `etl.vmap(graph, in_axes=0)`, run with a batched concrete built via `SparseTensor.from_parts` (the validating constructors reject batched leaves) with dense_shape UNCHANGED.
- **Batch-Dim identity:** the output-side static leaf at the sparse node's dense_shape[0] is the SAME `core.Dim('batch')` object the mapped tensor specs carry; input-side statics never gain the batch.
- **Batched to_dense:** only the Dim-at-position-0 batched concrete (output convention) materializes; the input-side batched convention (dense_shape unchanged) exists purely as a `run()` input — `to_dense` on it is NOT contracted (do not assert it).
- **grad argnums:** `etl.grad(f)` on a sparse arg needs `argnums=(1,)` (flat leaf position of the values; the int64 indices leaf at 0 is not differentiable — `argnums=(0,)` raises a pinned TransformError). Run grad graphs with the sparse node passed DIRECTLY (not wrapped in a tuple).
- **Bind:** leaf-level bind works by naming the leaf TensorSpecs (e.g. `name="idx"`/`name="val"` inside the SparseTensorSpec) and running with the unbound portion rebuilt via `from_parts`; whole-sparse bind fails with an unbound-portion structure-mismatch TraceError (pinned in test_pipeline.py). The values-leaf bind is guarded by the static dtype-leaf validation (path `[0][4]`).
- **Package-qualified imports:** `from tests.sparse.conftest import ...` (never `from conftest import ...`). Do NOT remove `tests/sparse/__init__.py` (module-name collisions + the `tests/numpy` shadowing rule apply to the whole suite).
- **Deferral wording to match:** capability drift = `"capability drift: the {backend} backend cannot execute sparse op '{op}' — sparse ops are numpy-backend-only in v1; densify with etl.sparse.to_dense"` (raised at `lower()` time, no compiler needed — do not gate on iree/tvm/xla availability); stablehlo export = `"stablehlo export: op '{op}' … is not supported in v1"`; scan-over-sparse-xs = `"etl.scan: xs leaf 2 must be a core.SymbolicTensor, got int"`; vjp deferrals = `"grad/vjp: vjp of {op} is a v1 deferral"`.
- **Inspection idiom:** `graph.module.functions[0].entry_block.ops` is the op list (same as tests/dist); op names compare as plain strings.
