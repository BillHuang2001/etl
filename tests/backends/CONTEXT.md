# tests/backends — backend-layer test suite

## Intent

pytest suites validating `etl.backends` (sibling: `../../etl/backends` — read its CONTEXT.md and `../../etl/CONTEXT.md` before changing expectations): the backend interface/registry/program contracts, the numpy interpreter backend (the numerical reference), the StableHLO exporter, and error/persistence behavior. Tests are the executable spec of the backend contract: graphs executed by the numpy backend must match direct numpy references computed with the SAME formulas as the kernels in `etl/backends/numpy/kernels/`.

## Files

| File | Area | Tests |
|---|---|---|
| `test_backend_interface.py` | `Capabilities` fields/defaults/frozen-ness, numpy_backend flags (dynamic_shapes/collectives/runtime_calls/custom_blocks True, async_collectives False) + dtype set, Backend ABC abstractness, Executable protocol, registry register/get semantics (idempotent re-register, duplicate-name, unknown-name, non-Backend, empty-name → BackendError), LoweredProgram/CompiledArtifact attrs + save/load smoke | 63 |
| `test_program.py` | numpy LoweredProgram `.text()`/`.payload` (serialized ir.Module dict), Signature fields (input/output TreeSpec, per-leaf TensorSpec, static_values, output_static_values default `()`), structured inputs (dict/dataclass), signature == `graph.signature_info()`, save/load round-trip, frozen Signature | 13 |
| `test_numpy_interpreter.py` | THE big numerical-correctness suite (~300 parametrized cases): elementwise arith/activations (erf via `math.erf`, erf-based gelu, sigmoid), comparisons/logical/bitwise/cast, reductions (axes/keepdims/dtype promotion, argmax/argmin), linalg (dot/solve/conv/tril/triu/cumsum — conv vs a manual cross-correlation reference), structure ops (reshape/transpose/slice/gather/scatter/concatenate/pad/select/broadcast), static getitem, enp sugar, operator overloads, evaluate/build/run ground truth | 303 |
| `test_symbolic_dims.py` | one-build/multi-size symbolic dims (B=3→5), DimExpr reshapes (B*2,), two symbolic dims transpose, None dims, determinism, mixed symbolic+concrete | 8 |
| `test_control_flow.py` | cond both branches + runtime predicate, while_loop iteration counts/0-iteration/early-exit, scan (cumsum), nested cond-in-while and while-in-cond, control flow under symbolic B | 11 |
| `test_runtime_call.py` | runtime_call sync execution (callback receives np.ndarray, called exactly once), scalar operands (arrive as 0-d float64), multi-output, dtype mismatch → BackendError, shape mismatch → ShapeError, wrong count → BackendError, missing callback registration → BackendError naming id | 10 |
| `test_collectives.py` | single-rank identity for all six collectives, rank/world_size defaults (0/1), multi-rank simulation via `set_collective_executor` hook (all_reduce sums, all_gather concatenates, arg forwarding incl. v1 protocol limitations), `rank_context=` kwarg + thread-local `set_rank_context`, hook set/get/reset (None → get raises BackendError, non-conforming → TypeError), RankContext validation | 22 |
| `test_stablehlo.py` | golden mnemonics (add/dot_general/reduce/reshape/transpose/concatenate/broadcast_in_dim/constant/if/while/collectives + module/func.func/return shell), dtype formatting (`tensor<2x3xf32>`, bool→i1), comparison_direction attrs, program-order emission, determinism, symbolic dims → `?`, decomposition emission (square/relu/stop_gradient/reduce_mean), deferred ops → BackendError naming the op (gather/scatter/scan/runtime_call/block_call/rank/world_size/argmax/argmin/erf/gelu + unmapped tril/triu/cumsum/solve), TypeError for non-graph input, dynamic-dim export rejection goldens (`_reject_dynamic_dims`: reshape operand/result, conv, slice, pad, keepdims-reduce reshape, reduce_mean dynamic reduced dim — message names op + offending dims + "dynamic"; reduce_mean dynamic NON-reduced dim still exports), dot_general mismatched-batch-rank goldens (rank-3@rank-2 non-batched, rhs-higher-rank lhs broadcast, size-1 rhs batch squeeze, dynamic size-1 rhs batch falls through to batched dynamic_broadcast_in_dim + batched dot_general, matched multi-batch direct, symbolic-batch dynamic_broadcast_in_dim + get_dimension_size, unprovable symbolic merge → BackendError naming "dynamic"), mixed-dtype operand equalization goldens (regression for 6aec043: `stablehlo.convert` insertion for elementwise/compare/select — e.g. `bitwise_and(cast(x, i32), 3)` → convert i32→i64 + `stablehlo.and` in i64, compare operands promoted to `np.result_type` (never i1), select pred stays i1 — plus the negative guard that matching-dtype graphs emit NO convert), per-algorithm SET-form `rng_bit_generator` goldens (mixed threefry+philox module: `{"threefry2x32"}` → native THREE_FRY + inline philox, `{"philox4x32_10"}` → native PHILOX + inline threefry; splitmix64 stays inline under the set form too) | 97 |
| `test_mixed_dtype.py` | mixed-dtype compile+run regression (fix 6aec043): the six graphs mixing dtypes on binary elementwise/compare/select (`add(i32, 3)`, `equal(i32, 3)`, `select(bool, i32, i64)`, `bitwise_and(i32, i64)`, `add(i8, i16)`, `add(f32, f64)`) must `etl.build`+`etl.run` on every available compiler adapter — iree/tvm/xla (CPU) — with numpy-backend parity (exact `np.array_equal` for int/bool, allclose 1e-5 for float) and dtype parity; per-backend skip gates (`pytest.importorskip` for iree deps `iree.compiler`/`iree.runtime` and tvm deps `tvm`/`jaxlib`; xla gate = `xla.register()` probe → `pytest.skip` with actionable message when no user-provided PJRT plugin is configured, runs for real with `ETL_PJRT_PLUGIN`/`plugin_path`); standalone (no `_adapter_utils` import) | 6 per backend × 3 = 18 (12 run + 6 xla-skipped in this env) |
| `test_options_override.py` | backend options-override contract (the backend half of `etl/backends/options.py` + per-adapter `KNOWN_OPTIONS`; env half = `tests/test_pipeline_options.py`): `validate_options` union semantics (a key valid for another stage passes, accepted-and-ignored) + unknown-key `BackendError` with the per-stage sorted listing substrings (`run: (none)` for empty sets), per-adapter `KNOWN_OPTIONS` declarations, iree compile-flag policy (`_resolve_iree_compile_args`: defaults survive, name-collision user flag drops the default, unknown flags pass through, deny-list flags rejected with the documented reason), iree cuda-runtime-flag suppression (`_configure_cuda_runtime_flags`: user `iree_runtime_args` / `IREE_PY_RUNTIME_FLAGS` mention → suppressed, else the default is parsed once — via a fake `iree.runtime` module, no IREE dependency) + `_apply_iree_runtime_args` loud rejection, iree llvm-cpu end-to-end (user compile args + load runtime args, run-stage unknown option → BackendError, explicit > env E2E with an env flag iree-compile rejects, reserved `sort_emission`/`while_init_rewrite` lower options accepted and working), tvm `tvm_target` accepted + version-adaptive `tvm_pass_configs` (forwarded on builds accepting the kwarg, loud BackendError naming the option otherwise), xla against the fake PJRT plugin (`plugin_path` honored at compile+load, `xla_compile_options` bytes accepted / non-bytes rejected before plugin work, run-stage unknown → BackendError, discovery error naming `ETL_PJRT_PLUGIN`), numpy documented-ignore (`rank_context` still flows). iree/tvm sections per-test importorskip-gated (pure validation + helper tests run everywhere); xla section builds `_fake_pjrt_plugin.c` like `test_pjrt_ctypes_plugin.py` (skips without a C compiler) | 38 |
| `test_while_init_rewrite.py` | iree-llvm-cpu regression for the while-init constant rewrite (fix 71d721e/ecb2e82, exporter option `while_init_rewrite`, default ON): all-zero rank≥1 constant while-INITs are replaced with COMPUTED zeros (`not`/`and`/dtype-changing `convert`/`reduce` over an input-derived one-axis-larger source). MLIR-position assertions (the while INIT is the reduce result; the all-zero constant is never consumed by the while), 10/10 sequential iree-llvm-cpu compiles with zero SEGVs on the tiny graph + bit-exact run, the REAL evox `non_dominate_rank` shape (input-derived (n,n) i1 dominance matrix + intra-body reduction + bool→int32 cast chains, n=32) bit-exact on llvm-cpu (vs both the numpy backend AND an independent front-peeling numpy reference), and the `while_init_rewrite=False` opt-out (lower() accepts it; MLIR-text assertion only — the opt-out shape is the upstream iree AffinityAnalysis SEGV and is deliberately never compiled) | 4 |
| `test_count_argsort.py` | count-mode argsort regression (fix 71d721e, exporter option `sort_emission` "pair"|"count"|"auto"; iree adapter default "auto"): the count composition emits NO `stablehlo.sort` (iota/compare/reduce ops asserted present); `"auto"` picks count iff the sorted-axis extent ≥ 32 (incl. a no-option iree `lower()` payload — the adapter default); llvm-cpu parity bit-exact vs the STABLE numpy references (`np.argsort(kind="stable")`, descending = flip — the count composition is inherently stable, and `stable=False` quicksort may order ties differently) for ascending/descending/tie-heavy-int/axis-0/`stable=True`; the upstream-bug path — argsort axis 64 bit-exact on iree-cuda (2 cases, GPU-guarded via an nvidia-smi most-free-device scan) | 11 (2 GPU-guarded) |
| `test_nsga2_xla_route.py` | the xla-route backend-switch evidence (fix 71d721e): argsort axis ≥ 32 (the iree count-mode trigger class) must build+run on the xla adapter bit-exact vs numpy — xla handles axis ≥ 32 sorts natively (no stablehlo.sort bufferization bug, no while-init SEGV). Per-test gate `xla.register()` probe → `pytest.skip` with an actionable message when no user-provided PJRT plugin is configured (`ETL_PJRT_PLUGIN`/`plugin_path`; same gate as test_mixed_dtype/test_adapter_xla) | 2 (skipped without a plugin) |
| `test_deferred_errors.py` | numpy lower-time rejection: block_call with no numpy impl and no portable decomposition → BackendError at `etl.lower` (verify() passes first — genuinely lower-time); compile backend mismatch; load with GPU device → BackendError/DeviceError; stablehlo NOT a registered backend; numpy capability probe (nothing standard is deferred) | 8 |
| `test_artifact_persistence.py` | LoweredProgram/CompiledArtifact/Executable save/load round-trips (incl. re-run after load), corrupt/truncated/wrong-payload-type files → PersistenceError, unregistered recorded backend → PersistenceError, explicit backend-name mismatch at load → PersistenceError, device mismatch (GPU) → BackendError, runtime_dependencies tamper → PersistenceError, resave idempotence | 11 |
| `test_compiler_framework.py` | shared `etl.backends.compiler.CompilerBackend` contract via a test-only stub subclass: `lower()` produces a `LoweredProgram` with payload `{"format": "stablehlo", "format_version": 1, "mlir_text", "entry_functions"}` (module/func.func @main/stablehlo shell, JSON-serializable); parametrized capability pre-checks naming the feature (collective/runtime_call/dynamic/float64/block — `graph.verify()` passes first); full-capability lowering of exportable graphs (runtime_call deliberately absent — no stablehlo mapping); `compile()` validates `lowered.backend == self.name` (BackendError naming both); registry + pipeline string-name resolution; LoweredProgram/CompiledArtifact save/load round-trips | 14 |
| `test_adapters_optional.py` | ALWAYS runs, no adapter deps, order-independent (env-sensitive assertions in fresh subprocesses via `sys.executable`): importing etl never imports iree/jax/jaxlib/tvm; unknown-name `get` → BackendError listing registered backends WITHOUT probing adapter modules; simulated missing dep (sys.modules sentinel + patched `find_spec`) ⇒ BackendError with install hint (`etl[iree]`/`etl[xla]`/`etl[tvm]` or the package name); present dep ⇒ `get()` lazily imports + registers the adapter; default backend stays numpy | 7 |
| `_adapter_utils.py` | shared helpers for the three adapter test files (graphs, fixed-seed inputs, cross-compiler fp32 tolerance, `stage`/`assert_parity`, `symbolic_softmax`/`symbolic_slice`/`symbolic_pad` rejection graphs, `batched_dot_cases`/`batched_dot_symbolic`) — NOT collected (underscore prefix; import package-qualified) | n/a |
| `test_adapter_iree.py` | optional IREE adapter contract via the shared CompilerBackend framework: registration/singleton, name-string resolution through lower/build/evaluate, numerical parity vs numpy backend, symbolic dims iff `dynamic_shapes` else BackendError naming the feature, symbolic dynamic-reshape rejection at `etl.lower` (softmax-style graph — etl's own BackendError, never a compiler parse error), StableHLO payload contract, artifact save/load round-trips (pipeline + raw `run([flat tensors])`), name mismatch ⇒ PersistenceError, capability rejections naming the feature, batched dot_general parity for mismatched batch ranks (rank-3@rank-2, rhs-higher-rank, size-1 rhs batch at 1e-3 K=768 fp32 tolerance) + symbolic-batch (B=2, B=5) via one compiled executable + vmap shared-weight dot (symbolic batch, size-1 rhs batch) compile-and-run parity | 16 |
| `test_adapter_xla.py` | same contract as the IREE file, against the XLA adapter (`jaxlib` CPU PJRT via ctypes, not the `jax` package) | 10 |
| `test_adapter_tvm.py` | same contract as the IREE file, against the TVM adapter (apache-tvm), plus dynamic slice/pad rejection at `etl.lower` (writer `_reject_dynamic_dims` surfaces as etl's own BackendError through the tvm path) | 13 |
| `test_iree_random_algorithms_parity.py` | iree-llvm-cpu round-trip parity for the algorithm-aware RNG lowering (threefry2x32 / philox4x32_10, commit 119854b): the iree adapter DEFAULT is native THREE_FRY for threefry (`Capabilities.rng_bit_generator=frozenset({"threefry2x32"})` — bit-exact on llvm-cpu + cuda, ~1.6-2.1× faster, see `etl/bench/rng_bench.py`), pinned by a default-path test (lowered MLIR contains `stablehlo.rng_bit_generator` + bit-exact run); the inline uniform/randint/permutation cases are bit-exact vs `etl.evaluate` (one compile per (case, algorithm), all raw-word keys looped incl. (0,0), 0xFFFFFFFF -1s, mixed, seed-derived) and are pinned to the INLINE path explicitly via the per-call `rng_bit_generator=frozenset()` lower override (the override flow is asserted on the lowered MLIR); f64 normal bit-exact + f32 normal within the documented fast-path budget `RANDOM_NORMAL_TOL` (incl. cross-run determinism), split/split_n → the algorithm's key shape ((2,)/(4,) i32) bit-exact (inline-pinned); the default philox path stays inline (no `rng_bit_generator` in the MLIR) and bit-exact; forcing native PHILOX on iree fails cleanly at compile (`BackendError` carrying the iree legalization diagnostics — never silent); the native `stablehlo.rng_bit_generator` THREE_FRY path via a manual `LoweredProgram` (native == inline == numpy), cross-roundtrip determinism, 3 iree-cuda smoke tests (GPU-guarded, device 5). No xla tests (xla `rng_bit_generator={"threefry2x32","philox4x32_10"}` runs stay gate-skipped without a PJRT plugin) | 29 |

## Conventions (binding for this directory)

- Run from repo root: `python3 -m pytest -q tests/backends` (root `conftest.py` handles sys.path). CPU only, numpy-only deps, small shapes, <2s per file. Total runtime ~2.4s.
- Execute graphs via `etl.evaluate(fn, *args, backend=etl.backends.numpy_backend)` or `etl.build`+`etl.run`; results are `etl.Tensor` — unwrap with `.numpy()`.
- Numerical references re-implement kernel formulas (never import etl kernels): `math.erf` for erf, `0.5*x*(1+erf(x/sqrt(2)))` for gelu, manual dilate→pad→cross-correlate loops for conv.
- Exact equality for int/bool; float32 rtol=atol=1e-6, float64 rtol=atol=1e-12; result dtype asserted.
- Documented etl quirks encoded as tests (do NOT "fix" etl here): `etl.slice` lengths are spans (limit = start+length, numpy `x[s:s+l:st]` semantics); `solve` requires rhs `(..., n, k)` for batched `a`; getitem supports only static int/slice; `dot` requires rank ≥ 2; etl.scan desugars to while+gather+scatter at trace time (its stablehlo export error names `gather`).
- Shared state hygiene: collective-executor hook and rank context are process/thread global — tests that touch them use fixtures to save/restore.
- **Adapter test conventions** (`test_adapter_{iree,xla,tvm}.py` + `_adapter_utils.py`): these pin the SHARED contract of the `etl/backends/compiler.py` CompilerBackend framework + the three optional adapters (`etl/backends/adapters/`). Each file is structurally identical, guarded by module-level `pytest.importorskip(<dep>)` calls BEFORE the etl imports, uses `NAME = "iree"/"xla"/"tvm"`, and shares all graphs/inputs/tolerances via `_adapter_utils` (import as `from tests.backends import _adapter_utils as u`). Real compilers run, so the <2s-per-file rule does NOT apply — keep compile count low (one shared module-scoped `adapter_artifact` fixture + one compile per distinct graph); fp32 parity tolerance is 1e-5 (cross-compiler), not 1e-6.
- **BUG(etl) policy:** when a test exposes a real etl bug, keep it FAILING with a `# BUG(etl): <description>` comment and report it upward with a minimal repro. Never fix etl and never weaken the test.

## Known Issues

- v1 collective-protocol limitations (asserted as-is): `reduce_scatter`'s `reduce_op` is not forwarded to the executor; `all_to_all` forwards only `split_axis`; `dist.broadcast` records the default `src_rank=0` in IR even when the user passes another value.
